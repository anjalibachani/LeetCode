class Solution {
    public int[] runningSum(int[] nums) {
        int len = nums.length;
        int[] answer = new int[len];
        int sum = 0;

        for(int i = 0; i < len; i++){
            answer[i] = nums[i] + sum;
            sum = answer[i];
        }
        return answer;
    }
}